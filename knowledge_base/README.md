# EcoSort AI Knowledge Base

This folder holds optional **static reference material** for demonstrations (e.g., sample municipal guidelines as `.txt` files).

The live RAG pipeline indexes documents uploaded through the **Document / RAG** page into a local ChromaDB store under `backend/chroma_data/`.

No external historical waste datasets are required for this project.
